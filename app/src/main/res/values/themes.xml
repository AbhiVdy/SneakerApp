<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SneakerApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SneakerApp" parent="Base.Theme.SneakerApp" />

    <style name="styleTitleText" parent="TextAppearance.AppCompat">
        <item name="android:textSize">@dimen/sp_22</item>
        <item name="android:textColor">@color/peach</item>
        <item name="textAllCaps">true</item>
    </style>

    <style name="styleCaptionText" parent="TextAppearance.AppCompat">
        <item name="android:textSize">@dimen/sp_20</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">@font/montserrat_semibold</item>
    </style>

    <style name="styleSizeButton" parent="Widget.AppCompat.Button">
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:paddingStart">18dp</item>
        <item name="android:paddingEnd">18dp</item>
        <item name="android:layout_marginStart">5dp</item>
        <item name="android:layout_marginTop">@dimen/dp_16</item>
    </style>
</resources>